# dots

Personal dotfiles, managed with [yadm](https://yadm.io).

## Bootstrap

On a fresh machine, run:

```sh
curl -fsSL https://raw.githubusercontent.com/lonetis/dots/main/.init.sh | bash
```

This installs Homebrew and yadm, then clones this repo with `yadm clone --bootstrap`.
